[build]
  command = "ls -l dist/backups"
  publish = "dist"

  # Environment variables
  [build.environment]
    NODE_ENV = "production"

[context.production]
  command = "ls -l dist/backups"
  publish = "dist"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
