[build]
  command = "rm -rf dist/*"
  publish = "dist"

  # Environment variables
  [build.environment]
    NODE_ENV = "production"

[context.production]
  command = "rm -rf dist/*"
  publish = "dist"

[[redirects]]
  from = "/"
  to = "/index.html"
  status = 200
